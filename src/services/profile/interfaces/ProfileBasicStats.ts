export default interface ProfileBasicStats {
    totalAnime: number,
    totalMovies: number,
    episodes: EpisodesStats
}

interface EpisodesStats {
    totalTime: number;
    totalEpisodes: 16106
}